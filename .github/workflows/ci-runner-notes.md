# CI Runner Notes

The workflow is configured to run on GitHub's hosted `ubuntu-latest` runner.

- No local runner setup required
- The build uses a Linux environment for Android Gradle compatibility
