# CI Status Badge

A status badge can be added later to the README after the workflow has run successfully.

Example badge syntax:

```markdown
![Android CI](https://github.com/<owner>/<repo>/actions/workflows/android-ci.yml/badge.svg)
```
