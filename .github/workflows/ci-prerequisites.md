# CI Prerequisites

To run the workflow successfully:

- Ensure the Gradle wrapper is committed
- Do not commit local build artifacts
- Use the `ArogyaSahayaV2` project directory for builds
- Keep GitHub Actions YAML files in `.github/workflows`
