# Android CI Build Steps

The configured workflow performs these steps:

1. Checkout repository
2. Set up JDK 17
3. Grant execute permissions to the Gradle wrapper
4. Run the build command in `ArogyaSahayaV2`
